# ================================================
# Supabase PostgreSQL Connection (Friend's DB)
# ================================================
# Direct Connection (Works because host supports IPv6)
spring.datasource.url=jdbc:postgresql://db.shwfqfuuhzvbhtpzqdbs.supabase.co:5432/postgres?sslmode=require
spring.datasource.username=postgres
# ⚠️ ASK YOUR FRIEND for the Supabase DB password and paste it below:
spring.datasource.password=Minbl23!mkbn
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate — use update so it creates your tables if they don't exist
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

server.port=8080
spring.application.name=swap-system
